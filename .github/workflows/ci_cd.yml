name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        working-directory: tracknest
        env:
          BOT_TOKEN: dummy_token_for_tests
          DB_USER: dummy_user
          DB_PASSWORD: dummy_password
        run: pytest tests/

  deploy:
    name: Deploy
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      # TODO: add your deploy steps here.
      # Common options:
      #   - SSH into a VPS and restart the bot process
      #   - Push to Railway / Render / Heroku via their CLI
      #   - Build and push a Docker image
      #
      # When ready, expose secrets like this:
      #   env:
      #     BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      #     DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      - name: Deploy (not configured yet)
        run: echo "Deploy step not configured yet — add your steps above."
